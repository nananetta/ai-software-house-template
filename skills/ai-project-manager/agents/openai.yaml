interface:
  display_name: "AI Project Manager"
  short_description: "Coordinate role-driven product delivery work"
  default_prompt: "Use $ai-project-manager to assign repository roles, coordinate handoffs, and drive this product task through the software-house workflow."

policy:
  allow_implicit_invocation: true
